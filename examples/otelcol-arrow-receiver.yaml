receivers:
  otelarrow:
    protocols:
      grpc:
        endpoint: "0.0.0.0:9002"

exporters:
  debug:
    verbosity: detailed

service:
  telemetry:
    metrics:
      level: none

  pipelines:
    traces:
      receivers: [otelarrow]
      exporters: [debug]
